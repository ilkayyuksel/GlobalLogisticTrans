# Generated or vendored — never hand-formatted.
node_modules/
dist/
build/
.next/
coverage/
pnpm-lock.yaml

# Prisma owns the formatting of these:
#   *.prisma        -> `pnpm db:format` (prisma format)
#   migrations/*.sql -> generated by `prisma migrate`, plus hand-written DDL
*.prisma
prisma/migrations/

# Documentation is deliberately hand-formatted (one line per sentence, blank
# lines between). Prettier's markdown printer would reflow it and produce a
# very large, meaningless diff across every document.
*.md
README

# Runtime artefacts.
storage/
